<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">

    <Table id="capturesTable" items="{captures}">
        <columns>
            <Column><Text text="Pokémon" /></Column>
            <Column><Text text="Weight (kg)" /></Column>
            <Column><Text text="Height (cm)" /></Column>
        </columns>
        <items>
            <ColumnListItem type="Inactive">
                <cells>
                    <Text text="{PokemonName}" />
                    <Text text="{Weight}" />
                    <Text text="{Height}" />
                </cells>
            </ColumnListItem>
        </items>
    </Table>

</core:FragmentDefinition>
